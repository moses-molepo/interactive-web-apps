const freeWarning = 'Free shipping only applies to single customer orders'
const bannedWarning = 'Unfortunately we do not ship to your country of residence'
const noneSelected = 0
const customers = 1
const location = 'RSA'
const location2 = 'NAM'
const location3 = 'NK'
const currency = null
const batteries = 35

console.log(freeWarning)

if (location == 'RSA') { shipping === 400 && currency === 'R' }

if (location2 == 'NAM') { 
  shipping = 600 }
  
else {shipping = 800
    shoes = 300 * 1
    toys - 100 * 5
    shirts = 150 * noneSelected
    batteries = 35 * 2
    pens = 5 * noneSelected
    shipping = null
    currency = '$'
    }


if (shoes + batteries + pens + shirts > 1000  ) { location2 == 'NAM' && customers < 2 
		    let shipping = 0 || calcShipping
		}
	
      

if (shipping === 0 && customers !== 1) { 
    console.log(freeWarning)} 
else{
 location3 = 'NK' ? console.log(bannedWarning) : console.log(price, currency, shoes + batteries + pens + shirts + shipping)

}
